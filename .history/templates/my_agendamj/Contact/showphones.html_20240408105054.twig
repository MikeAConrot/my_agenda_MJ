{% extends 'base.html.twig' %}

{% block title_name %}
     AGENDA 2024
{% endblock %}


{% block navbar %}

<a name="" id="b" class="btn btn-primary" href="{{path('index')}}" role="button">HOME</a>
<a name="" id="b" class="btn btn-primary" href="{{path('contact_new')}}" role="button">NEW CONTACT</a>
<a name="" id="b" class="btn btn-primary" href="{{path('phones_new')}}" role="button">NEW PHONE</a>


{% endblock %}



{% block header%}
    <h1 style="text-align: center" class="mj">CONTACTS</h1>
{% endblock %}
 
{% block content  %}
  
    <!DOCTYPE html>
<html lang="en">
<head>
    
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css" integrity="sha384-HSMxcRTRxnN+Bdg0Jdbx8PpqyWggFhqmenPEMA7mz72Hvk mTn2vhyJXapM6DHiVrWxJE" crossorigin="anonymous">
    <link rel="stylesheet" href="styles.css"/>
</head>
<body>
    <div class="container">
        <table class="table table-bordered">
            <thead >
                <tr >
                    <th id="indicestabla">PHONE NUMBER</th>
                    <th id="indicestabla">TYPE</th>
                    <th id="indicestabla">MODIFY</th>
                </tr>
            </thead>
            <tbody>
                {% for phone in phones %}
                    <tr>
                        <td>{{ phone.name }} {{ phone.lastName }}</td>
                        <td>{{ phone.mail }}</td>
                        <td>
                            <a name="" id="bupdate" class="btn btn-primary" href="{{ path('edit_contacts', {'idContact': contac.id}) }}" role="button">Edit Contact</a>
                            <a name="" id="bdelete" class="btn btn-primary" href="{{ path('delete_contacts', {'idContact': contac.id}) }}" role="button">Delete Contact</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    <!-- Add the following lines to include Bootstrap JS -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js" integrity="sha384-aJ21OjlMXNL5UyIl/XNwTMqvzeRMZH2w8c5cRVpzpU8Y5/X5y63Np/g==" crossorigin="anonymous"></script>
</body>
</html>

{% endblock %}